# M0 designer notes · Agent Monitor 高保真变体

> 作者 lane 的自述。评审请另开 lane，不要读这份当结论。
> 交付：`design/tokens.css`、`design/variations.html`、`design/shots/*.png`。

---

## 1. 做了什么

按 `design-system-extract → generate-variations → make-a-prototype` 三步走。

**第一步 · 抽 token。** 从 `DESIGN.md`（raycast）逐字抽出 surface 四级阶梯、三种 hairline、
八个文本色、四个语义色、圆角与间距标度、Inter 及其完整 `font-feature-settings`。§1–§6 与
§9 的 Inter 栈没有一个值是我编的。然后追加项目层：身份色三只、额度阈值、为 960×540 重新
切的九级字号、动效时长与缓动、屏幕几何。文件末尾写了 findings——抽了什么、DESIGN.md 缺
什么、我替项目补了什么、发现的三处内部矛盾。

**第二步 · 定变体的轴。** 简报已经把三个轴定死了（Ledger / Gauge / Strip），我要做的是
把每个轴推到它自己的极端，并保证任意两个之间的差别能一句话说清。三条 caption 见 §3。

**第三步 · 做原型。** 单文件 `variations.html`，中央一块 960×540、1:1 不缩放的 stage，
上方一条工具样式的控制栏（刻意用 system-ui，跟项目字体拉开，提示它不参与设计）。
三个变体共用一份内联的 `tokens.css`，差异只在变体自身的 CSS 与结构；变体走 `data-variant`、
状态走 `data-state`，原生 JS，无框架。fixtures 内联在 `<script type="application/json">`，
不 fetch。`localStorage` 记住变体、状态与 reduced-motion。

`tokens.css` 与 HTML 里的内联副本不是手抄的，是构建步骤拼进去的，已断言逐字相同。

---

## 2. 字体：选 IBM Plex Mono，不选 JetBrains Mono

简报给的提示是「48px 以上的数字不能读起来像代码」，这条直接决定了结果。

JetBrains Mono 的 x-height 被刻意抬到接近 cap height，字怀开得很大——这是为 12–14px 的
代码行做的优化，目的是在小字号上把 `l/1/I`、`0/O` 撑开。同一套骨架放到 48px，抬高的
x-height 会让数字显得矮而胖，加上它标志性的点心零和高对比末端，读起来就是「终端输出」。

IBM Plex Mono 的数字继承 Plex Sans 的人文主义骨架，x-height 比例更常规，末端更平静，
48px 上是「被排版的数字」而不是「代码里的数字」；它和 Inter 的宽度比例也更接近，
`Claude Code 18%` 这种 Inter 标签 + Plex 数字的混排不会在基线上打架。

两者都在 Google Fonts 上，都带 `tnum`；`zero` 特性我一并打开了，因为副屏上会出现
`8/1000` 这种含零的计数。标签沿用 Inter + `ss03`，这是 DESIGN.md 的签名细节，没动。

代价我认：Plex Mono 在 12px 上不如 JetBrains 清楚。所以 12px 的那一层（轴标签、
事件时间）我控制在两三个字符的短串，不让它承载需要逐字符分辨的内容。

---

## 3. 三个变体

| | 差异轴 | 一句话 |
|---|---|---|
| **V1 Ledger** | 数字优先，等高行 | raycast 原样：百分比是主角，两条细 bar 做趋势，事件是等高行。信息密度最高、认知成本最低，也最没有记忆点。 |
| **V2 Gauge** | 形状优先，节奏分层 | 5h 窗换成 270°（开口朝下）弧形仪表，倒计时落在仪表中心，身份色只上进度弧；最新一条事件放大成 22px 双行卡，其余压到 40px。读「还剩多少」从读数字变成读形状。 |
| **V3 Strip** | 时间优先，同轴对照 | 主轴从「用了多少」换成「时间走到哪」：每个 agent 一条横向 5h 时间带，额度填充与「现在」游标画在同一根轴上；事件流改成共用时间刻度的竖向时间轴。 |

任意两个之间的一句话差别：
V1→V2 是「同一份信息，从读数字改成读形状」；
V2→V3 是「从读还剩多少，改成读够不够撑到重置」。

### 推荐 V3

理由只有一条，但是硬的：**V1 和 V2 都只回答「还剩多少」，而这块屏真正该回答的是
「够不够撑到重置」。** V3 把这个判断直接画了出来——时间带上有两个标记，额度填充的右端
和「现在」游标，两者的落差就是燃烧速度。填充落在游标后面＝用不完，跑到游标前面＝会提前
用尽。这是另外两个变体给不出的信息，用户只能自己心算「22% 用掉了，可 5 小时窗已经过了
一半」。

edge 态是最好的证据：Codex 97% 看着吓人，但窗口已经走到 99%，V3 直接写「消耗落后时钟
2 个点 · 按这个速度用不完」；Claude 只有 82%，窗口才走到 51%，V3 写「消耗跑在时钟前面
31 个点 · 按这个速度会提前用尽」并转黄。同一屏上，看起来更危险的那个其实没事，看起来
安全的那个才要减速——V1 和 V2 表达不了这个反转。

附带好处：V3 的时间轴一屏多放得下一条事件（1 放大 + 4 行，V1/V2 是 1 + 3）。

风险：5h 时间带在 7 寸屏上只有 20px 高，实机上可能偏细；如果实测读不出来，退而求其次
选 V2——它的仪表弧在远距离上是最强的预注意信号。

---

## 4. 简报没写、我自己定的决定

1. **时间原点冻结在 fixtures 的 `generatedAt`（2026-09-14 14:33:21 +08:00），之后按真实
   经过秒数递增。** 否则倒计时会和 `quota.json` 里的 `resetsAt` 对不上，截图也不可复现。
   时钟仍然逐秒在走，能看到 tabular 数字换位时不抖。
2. **accent 会随年龄衰减。** 只有最上面那条「未读 / 需要你」拿全套强调（surface 升一档 +
   标题转 ink 加粗 + 6px ink 圆点）；第二条及更老的未读降级成 `--accent-dim` 的圆点。
   否则连点四次「模拟新事件」就有四处 accent，纪律就成了摆设。
3. **事件流的未读计数从 ink 降到 body。** 它本来会白白吃掉第二个 accent 名额，而未读行
   自己已经在说同一件事。
4. **V3 的 attention：顶部 strip 接管之后，事件流不再重复这一条。** 不然同一件事会占掉
   strip、瓦片标记、事件行三个名额。现在稳定是 2 个：strip + 对应 agent 的瓦片标记。
5. **`--ash` 收得比简报更紧。** 简报允许 ≥18px 用 ash，但 ash 在 `--surface` 上实测
   3.64:1，未达 4.5:1 正文线（WCAG 的「大字」是 18pt≈24px，不是 18px）。所以所有有意义的
   小字一律用 `--mute`（7.09:1），ash 只留给非信息性的刻度标记。
6. **focus 用 2px `--ink` 内描边，不用 `--hairline-strong`。** 简报给了两个选项，但
   hairline-strong 叠在 `--surface` 上实测 1.56:1，达不到 WCAG 1.4.11 对 focus 指示器的
   3:1 要求。ink 是 17.05:1。这是纠正，不是偏好。
7. **cwd 从头部收，保留末尾 2–3 段。** 一开始用 `direction:rtl` 做反向截断，结果把
   `~` 甩到行尾（显示成 `…ojects/dsh-ime-demos/~`），读起来是坏的。改成按半角宽度计算
   （CJK 记 2）从头收，现在是 `…/Projects/dsh-ime-demos`。
8. **放大行没有 summary 时，副行让给 cwd，右侧 cwd 列同时留空**，不在同一行里把同一段
   路径印两遍。
9. **V3 的百分比是 18px，不是 48px。** 简报的屏幕规则写了主数字 ≥48px，但 V3 的变体轴
   明确要求「额度百分比退为次级」——两条已定项在这里冲突，我按更具体的那条执行，并把
   数字从最初的 16px 提到 18px，保证手臂距离仍然读得清。V1 / V2 的主数字都是 48px。
10. **每个变体按可用高度定了行数预算**（V1/V2 = 1 放大 + 3 行，V3 = 1 放大 + 4 行），
    超出的条数写在事件流表头（「8 未读 · 另 4 条更早」），不做滚动——副屏上没有滚动的
    交互前提。
11. **loading 用静态骨架，没有 shimmer。** 动效只服务状态变化，循环的微光是装饰。
    骨架旁挂一个 `role="status"` 的「读取中」。
12. **提示音：G5 (784Hz) + D6 (1174.7Hz) 两个正弦，纯五度，180ms 指数衰减，峰值增益
    0.055。** 五度比三度更不像「通知音效」，更像一次轻敲。
13. **running 在事件流里占一条置顶的「进行中」行**，计时逐秒递增；瓦片那边是徽记旁的
    2s 脉冲。两处都有，是因为「谁在跑」既是 agent 的状态也是时序里的一件事。
14. **edge 态里有两处是合成的**，本机数据里不存在：Codex 97% / 3 分钟后重置（阈值演示），
    以及「一分钟内 8 条」的时间戳（真实记录跨了好几天）。两处都基于真实记录改写，标题、
    cwd、agent 归属没动，代码里有注释标明。其余所有事件与额度都直接来自 fixtures。

---

## 5. 怎么验的

Playwright + Chromium，视口 1100×820，`deviceScaleFactor: 2`，逐个走完 **3 变体 × 7 状态
= 21 个组合**，每个组合跑四类断言：

- stage 内所有后代元素的 bounding box 必须落在 960×540 之内（越界即 FAIL）
- `.feedlist / .tiles / .screen / .tile / .tile-foot / .b-head / .b-axis / .row`
  的 `scrollHeight/scrollWidth` 不得超过 `clientHeight/clientWidth`
- stage 内不存在计算字号 < 12px 的文本节点
- 数一遍全强度 accent（`rgb(244,244,246)` 的未读点 + attention 瓦片 + 可见的 strip）

**最终结果：21/21 通过，0 console error，0 布局问题。** accent 实测分布：
populated / edge / running 各 1 处，attention 2 处，loading / empty / error 各 0 处。

交互也是实跑的：
- 「模拟新事件」→ 行数受预算封顶（4→4），表头从「1 未读 · 另 2 条更早」变成
  「2 未读 · 另 3 条更早」
- Tab 两下进入 stage 第一行，`box-shadow` 实测 `rgb(244,244,246) 0 0 0 2px inset`
- Enter 触发 ack，表头回到「1 未读」，`aria-live` 播报「已标记已读，还剩 1 条未读」，
  焦点留在原行
- 切到 V3 / edge 后 reload，`data-variant=v3 data-state=edge` 正确恢复
- 勾选 reduced-motion 后 `.iddot` 的 `animationName` 实测为 `none`

另外断言过：内联 tokens 与 `design/tokens.css` 逐字相同、两份 fixtures 逐字内联、
无 `fetch()`、无本地 css `<link>`、无 emoji、无 `linear/radial/conic-gradient`、
无 Tailwind 靛蓝系、无裸 `#000000` / `#ffffff`。

截图（`design/shots/`，每张 1920×1080 = 960×540 @2x）：
`v1-populated.png` `v1-attention.png` `v2-populated.png` `v2-attention.png`
`v3-populated.png` `v3-attention.png`

---

## 6. 没能验证的

1. **实机。** 所有判断都是在 Mac 的 Chromium 上按 960×540 逻辑像素做的。7 寸面板、手臂
   距离、@2x 的真实观感没试过。最可能翻车的三项：V3 时间带 20px 高会不会太细、
   12px 那一层在实机上是不是真的能读、48px 数字会不会反而过大。
2. **提示音。** Playwright 是 `--mute-audio` 跑的，只验证了 Web Audio 路径不抛异常，
   **没有真的听过**。音色、响度、在副屏喇叭上会不会太尖，都要实机再调。
3. **V2 仪表的轨道对比度。** 简报定了「轨道用 hairline」，我照做了。但 `--hairline`
   `#242728` 叠在 `--surface` 上实测 **1.29:1**，远低于 WCAG 对非文本图形的 3:1。
   实际效果是轨道几乎看不见，弧的「总长度」要靠猜。现在能读，是因为中心有倒计时、
   右边有 48px 数字兜底。**这条建议在 M1 实机上复核，必要时把轨道提到 `--stone`
   （1.97:1）或更高。** 我没有擅自改已定项。
4. **字体加载失败的样子。** Inter 与 IBM Plex Mono 都从 Google Fonts 拉。Electron 渲染层
   如果离线，会落到 fallback 栈（`system-ui` / `ui-monospace`），字宽和 x-height 都会变，
   48px 数字的负字距可能需要另调。没有模拟过离线渲染。
5. **屏幕阅读器。** `aria-live` 的播报文案写了，`role="status"` 的容器也先于内容存在，
   但没用 VoiceOver 实际听过播报顺序。副屏监视器大概率不是读屏用户的场景，优先级不高，
   但没验就是没验。
6. **系统级 `prefers-reduced-motion`。** 验的是 CSS 媒体查询规则本身 + 控制栏开关；
   没有真的去 macOS 系统设置里打开「减弱动态效果」再看一遍。
7. **长时间运行。** 逐秒的增量更新只改文本和 `left`，不重建 DOM（所以脉冲和呼吸不会被
   打断），但没有跑够长的时间去看 24h 后有没有累积误差或泄漏——那是 M4 的验收项。
8. **真实的 attention 数据。** 「等待你批准」这一档在 PLAN.md 里排在 M4，本机现在没有
   任何真实样本。那条 attention 事件的文案是我照 Claude Code 的 `permission_prompt`
   语义写的，实际字段拿到之后要回头核一遍措辞长度。

---

## 7. 建议的下一步

按你定的链路，接下来应该是 polish-pass 的四项审查（accessibility-audit /
ai-slop-check / hierarchy-rhythm-review / interaction-states-pass），由另一个 lane 的
reviewer 用 `design-vocab.md` 的机制词汇来做，我不自评。

评审时最该被挑战的三处，我先列出来省得被绕过去：
- §4.9 V3 主数字 18px 与「主数字 ≥48px」的冲突，我的处理对不对；
- §6.3 V2 仪表轨道 1.29:1，照做已定项 vs 满足 3:1，哪个优先；
- §4.2 accent 随年龄衰减这条规则是不是过度设计——它让「未读」有了两个视觉等级，
  可能反而增加了学习成本。

---

# 返工记录（第一轮评审 · 960×540 画布）

依据 `01-slop-hierarchy.md`、`02-a11y-states.md` 与 `brief-m0.md` 末尾的「评审后修订」。
**B/Q/P 共 15 条，全部修完，0 条未修。** 本节写的是 960 版的改动；这些改动随后
原样带进了 3.5 寸版（见下一节），没有任何一条被回退。
960 版留存于 `design/variations-960x540.html`，截图在 `design/shots/960/`。

## 阻断

**B1 · 轨道类图形一律 ≥3:1**
`--quota-track` 从 `--stone`（1.97:1）提到 `--ash`（3.64:1），并新增 `--quota-fill: --ink`。
落到四处：V1 的 bar 轨、V2 的仪表弧轨、V3 的时间带轨、V3 时间轴的已读节点。
这条改动有两个必要的连带修正，不做的话「提到 ash」反而会让信息更难读：
- 轨道变亮后，原来 `--body` 的填充与轨道只剩 3.28:1，边界发虚 → 填充统一提到 `--ink`，
  填充与轨道之间恢复到 4.86:1。
- V3 的刻度（原 `--stone`）会消失在 ash 轨道里，游标（原 `--ink`）会消失在 ink 填充里
  → 两者统一翻成 `--canvas` 的暗色，无论落在轨道上还是落在填充里都是一道高对比缺口。
- V2 的进度弧是身份色，橙 / 蓝叠在 ash 上按亮度只有约 1.7:1 → 进度弧比轨道宽 2px
  （3.5 寸版是 1px），用形状差补色差，不靠颜色单独传信息。
**验证**：Playwright 逐组合实测 V1 bar 3.64:1 / V2 arc 3.64:1 / V3 band 3.64:1 /
V3 node 3.75:1（已读）·17.69:1（未读），全部 PASS，已固化为断言。

**B2 · loading 的 `role="status"`**
四处「读取中」（三块瓦片 + 事件流）都加上 `role="status"`，并在进入 loading 态时向
`#live` 推一条「正在读取三个 agent 的额度与事件」。原先文件顶部注释和笔记都声称有，
实际没有——这是声明与实现不符，比缺功能更糟。
**验证**：loading 态 `[role="status"]` 计数 = 5（3 瓦片 + 1 事件流 + 1 #live），已固化为断言。

**B3 · stage 内的语义标题**
`.brand` → `h1`，三块瓦片名 → `h2`，事件流标题 → `h2`。用
`.stage :where(h1,h2){margin:0;font:inherit;…}` 重置，`:where()` 不贡献特异性，
所以原有类规则仍然决定全部视觉，**外观零变化**。
**验证**：21 个组合 heading 数均为 5，已固化为断言（≥5）。

**B4 · 事件行 aria-label 拼入 kind**
新增 `KIND_LABEL = {running:"运行中", failed:"失败", attention:"等待批准", completed:"已完成"}`，
同时把相对时间也拼了进去。kind 在视觉上由 `.rkind` 图标传达，而图标是 `aria-hidden`，
不拼进 label 读屏用户就分不清这条跑没跑完。
**验证**：running 行实测 `Codex · 运行中 · 构造 v0.3 盲判任务分类样本 · 4m23s`。

## 质量

**Q1 · `--id-codex` → `var(--charcoal)`（#d3d3d4）**
按 lead 的简报修订 1 执行。这一条同时解掉 R1-01 / R1-03 / R2-11 三个发现，
也给 `--charcoal` 这个「抽了没用」的 token（R1-08）找到了职责。
**验证**：新增断言「非 attention 瓦片上不存在 `rgb(244,244,246)` 的身份点」，21 组合通过；
accent 计数 populated / edge / running 各 1、attention 2、loading / empty / error 各 0。

**Q2 · attention 态下时钟不消失**
960 版：V3 的 topbar 从 `display:none` 改成 sr-only（`h1` 留在无障碍树里），
可见时钟由 strip 右端接管，topbar 那份标 `aria-hidden` 避免读屏听两遍。
**验证**：新增断言「任何变体任何状态下至少一个可见且非空的 `[data-clock]`」，21 组合通过。

**Q3 · V1 / V2 的阈值判语 + 警示图标**
`ok` 级保持沉默（不加填充文案）；`warn` / `danger` 时 `tile-foot` 的时钟图标换成 alert，
文案变成「2h26m · 余量偏紧」/「2m · 余量吃紧」。V2 把它做成 foot 里的独立 chip。
这补上了 R2-05 指出的缺口：V1/V2 的阈值原先只有颜色一个信号。
**验证**：v1 edge foot 实测「2m · 余量吃紧」；v2 三块瓦片实测
`["danger:2m · 余量吃紧","warn:2h26m · 余量偏紧","ok:"]`。

**Q4 · `.row:active`**
`background: --surface-card` + `inset 0 0 0 1px --ash`，`transition-duration: --dur-instant`(120ms)。
规则排在 `:focus-visible` 之前，键盘描边优先级更高。

**Q5 · V3 attention 的事件流表头**
strip 接管后表头原本会显示「全部已读」——单独看下半屏会以为没事要处理。
改成「1 项在上方等待」。
**验证**：实测 `1 项在上方等待`。

**Q6 · `.attn-tag` 字距** `--tr-caps`(.08em) → `--tr-small`(.01em)。它渲染的是中文「等待你」，
ALL CAPS 的 tracking 规则对 CJK 不适用。

**Q7 · attention 行边框** `--hairline-strong`(1.54:1) → `--ash`(3.64:1)。

## 打磨

**P1 · 间距回栅格** 29 处 gap / padding 收到 2/4/8/12/16/24/32。剩 5 处例外全部是字形几何
而非节奏（V3 `.tile` gap 2px、`--dot` 6px、徽记前导点 5px、游标 3px、时间轴节点 11/3px），
已在 `tokens.css` §6 逐条列名并说明理由。

**P2 · 跨阈值播报** 额度从 ok 跨到 warn / danger 时向 `#live` 推一句，只在跨越那一刻播一次，
不是逐秒念。**验证**：populated → edge 实测「Claude Code 5h 额度已达 82%，余量偏紧」。

**P3 · breathe 去掉 `filter:brightness`** 改成一层 `pointer-events:none` 的 ink 叠层动 opacity，
纯 GPU 合成，不再触发 paint。峰值 6% 不透明度把 `#101111` 在 sRGB 数值上抬约 14/255 ≈ 5.4%，
仍在简报的「亮度不超过 8%」之内。这里对「8%」采用 sRGB 数值口径而非线性亮度口径——
线性口径下 8% 在近黑面上肉眼完全不可见，那样的呼吸等于没有。

**P4 · V3 的 18px 主数字** 按 lead 的简报修订 3 予以接受，不改。3.5 寸版沿用同一取舍，
数字是 16px。

## 断言变更

新增四条，全部固化进 `verify.mjs`，21 个组合逐个跑：
轨道 ≥3:1 · attention accent ≤2 · stage 内 heading ≥5 · loading 存在 `role="status"`。
另外把「溢出」这条断言的语义改准了：原来对所有容器判 `scrollHeight > clientHeight`，
会把 `text-overflow:ellipsis` 的正常截断和 sr-only 的 1×1 裁切误报成失败。
现在只对结构容器判纵向裁切，横向逃逸交给 stage 边界那条，另加一条
「凡靠省略号收尾的元素必须真的 `overflow:hidden`」。

---

# 3.5 寸版

用户实机确认副屏是 **3.5 英寸**（不是 7 寸），1920×1080 物理 / 960×540 逻辑，
并在 1x / 1.5x / 2x 三档字号比对里选了 **2x**。按 `design/brief-m0-35.md` 重做。

## 为什么不是把 960 的版面缩小

960 那版是按 7 寸推的，字号下限也是按 7 寸定的。屏幕砍一半、字号不动，等于把所有
文本的物理尺寸砍一半——那正是用户觉得字小的原因。所以这一版是**重排**不是缩放：
画布改成 480×270 CSS px，整个 stage 挂 `zoom: 2` 铺满 960×540。
1 画布 px = 4×4 物理像素 = 0.16 mm（1920×1080 @ 3.5" ≈ 629 ppi），
比普通桌面显示器的 1 px（约 0.26 mm）还小 0.6 倍——换句话说，画布坐标里的 12px
在这块屏上看起来只有普通屏上 12px 的六成大。所以内容必须砍。

## 砍了什么

| 砍掉的 | 理由 |
|---|---|
| 品牌字「AGENT MONITOR」 | 它不回答那两个问题里的任何一个，纯占 28px |
| 事件流的卡片外壳与表头 | 省下 18px 的 padding + 30px 的表头，全给事件行本身 |
| 未读计数、「另 N 条更早」 | 屏上只剩 1–2 条事件，计数是在数看得见的东西 |
| 事件行的 cwd | 「在哪个项目」不是马上要行动的信息；它也是唯一一条因此不再适用的 edge 边界 |
| ZCode 空态的「怎么接」那句 | 按简报移到托盘菜单，屏上只留「未连接」+ 一行原因 |
| error 的两段式文案 | 原因与下一步压成一句 |
| V2 的 7d 窗、V1 的 7d 独立 bar | 7d 在 5h 窗面前永远是次要的 |
| ZCode 的 1mo（MCP 8/1000）窗口 | 三个窗口里最不紧急的一个，本画布放不下 |
| 事件流第 3–5 行 | 预算只够 1 条放大 + 1 条次新（V3 只够 1 条） |

留下的：三个 agent 名 + 状态点 + 5h 百分比（V1 48px / V2 44px / V3 16px）+ 重置倒计时
+ 一条放大的最新事件 + 右上角时钟。

## 三个变体

| | 砍法 | 一句话 |
|---|---|---|
| **V1 Ledger** | 7d 退成 12px 次级数字，只留一条 3px 细 bar | 48px 百分比一字排开，认得最快，也最没有形状记忆点 |
| **V2 Gauge** | 7d 整个不显示；60px 弧内只放倒计时，44px 百分比放弧旁 | 砍得最狠，但远处先看到弧的缺口，走近才读数字 |
| **V3 Strip** | 事件区只留一行；百分比退成 16px | 三条竖向时间带，填充与「现在」游标同轴，判语压到 12px 保留 |

两两差别：V1→V2 是「从读数字到读形状」；V2→V3 是「从读还剩多少到读够不够撑到重置」。

## 推荐：V1（相对 7 寸版的改判）

7 寸版我推荐的是 V3，理由是只有它把「够不够撑到重置」画了出来。3.5 寸上这个理由不成立了：
时间带只剩 **10px 高**，在 50–60 cm 外基本读不出填充右端与游标之间那点落差，
V3 最值钱的机制退化成一句 12px 的文字——而那句话 V1 的 `tile-foot` 也放得下
（返工 Q3 已经加了「2m · 余量吃紧」）。

在这个尺寸上，**48px 的数字是唯一能从座位上一眼读到的东西**，V1 把整块预算押在它上面，
这是最诚实的分配。V2 是第二选择：弧的缺口在余光里比数字更容易被注意到，
代价是 7d 完全看不见。V3 留作参考——如果之后换回大屏，它仍然是信息量最高的那个。

## 这一版新做的判断（简报没写的）

1. **瓦片头只留名字 + 身份点，状态词下沉到 `tile-foot`。** 149px 宽的头里塞不下
   「Claude Code」+「等待你」+ 点，实测名字被省略号吃成「Claude Co…」。
   现在 foot 按优先级显示：等待你批准 > 额度判语 > 运行中 · 倒计时 > 倒计时后重置。
   这样每个状态都仍有文字信号，不靠颜色单独传。
2. **百分号拆成 14px 的独立后缀。** 「22」用 48px、「%」用 14px 基线对齐。
   这不只是排版趣味——`56px 弧 + 44px「22%」` 在 127px 的瓦片内宽里放不下，
   拆开之后 `60 + 4 + 52.8 + 8.4 = 125.2` 刚好进得去，主数字才保住了 44px 底线。
3. **V2 在 ok + idle 时 `tile-foot:empty` 整条收掉**，仪表回到瓦片真正的垂直中心；
   有判语或状态词时它才出现。符合简报「ok 级保持沉默」。
4. **attention 不再另起全宽 strip**（960 版是 V3 专属的 strip）。3.5 寸上一条 44px 的
   strip 会吃掉半个事件区；改成最新事件行自己变成 attention 行，标题前缀「等待你」。
   accent 仍然是 2 处：attention 行 + 对应 agent 的瓦片标记。
5. **h1 与事件区标题转为 sr-only。** 品牌字按简报砍掉了，但文档大纲不能跟着塌，
   所以保留一个不可见的 h1 和一个不可见的 h2，heading 数仍是 5。
6. **V3 的百分比是 16px。** 沿用 lead 已接受的取舍（V3 的轴定义「额度退为次级」
   比通用的主数字底线更具体）。

## 验证

Playwright / Chromium，视口 1100×900，`deviceScaleFactor: 2`，**3 变体 × 7 状态 = 21 组合**，
断言与返工后完全一致（含新增四条）：

- stage 960×540 边界内无越界元素（sr-only 除外）
- 结构容器无纵向裁切；凡靠省略号收尾的元素都真的 `overflow:hidden`
- stage 内无计算字号 < 12px 的文本
- **轨道 ≥3:1**：V1 bar 3.64:1 · V2 arc 3.64:1 · V3 band 3.64:1，全部 PASS
- **accent ≤2**：populated / edge 各 1、attention 2、loading / empty / error 0
  （V3 的 running 态是 0——预算只放得下那条「正在运行」行，它本身不是未读）
- **heading ≥5**：21 组合均为 5
- **loading 有 `role="status"`**：计数 5
- 时钟在 21 个组合里都可见且非空

**结果：21/21 通过，0 console error，0 布局问题。**

交互实跑：模拟新事件（行数受预算封顶 2→2）· Tab 两下进入第一行，
`box-shadow` 实测 `rgb(244,244,246) 0 0 0 2px inset` · Enter ack 后 `#live` 播报
「已标记已读，还剩 1 条未读」· 刷新后 `data-variant` / `data-state` 正确恢复 ·
reduced-motion 勾选后 `.tile::before` 与 `.iddot` 的 `animationName` 均为 `none` ·
跨阈值播报实测「Claude Code 5h 额度已达 82%，余量偏紧」·
事件行 label 实测「Codex · 运行中 · 构造 v0.3 盲判任务分类样本 · 4m23s」。

截图覆盖 `design/shots/` 六张（960×540 @2x = 1920×1080 物理像素，与面板一一对应）。

## 这一版没能验证的

除了 7 寸版笔记里那份清单仍然成立（实机观感、提示音、字体离线 fallback、VoiceOver、
系统级 reduced-motion、24h 长跑）之外，3.5 寸带来三条新的：

1. **2x 这一档是用户在 `design/scale-test.html` 上选的，但选的是字号比例，不是这份版面。**
   版面重排之后的实际观感（尤其是 48px 数字会不会反而过大、12px 那一层是不是真的
   还能认）没有在实机上回看过。
2. **V3 的 10px 时间带我判断读不出落差，这个判断本身没有实机验证。** 它是推荐从 V3
   改判到 V1 的主要依据，如果实机上其实读得出来，这个推荐应该翻过来。
3. **`zoom: 2` 的渲染保真度。** Chromium 下 1px 边框与 3px bar 在 zoom 2 时是否稳定出整数
   物理像素、会不会出现半像素模糊，只在 Playwright 的截图上看过，没在 Electron 里看过。
   如果实机上发糊，退路是把画布写成 960×540 但所有尺寸按本版 ×2 硬编码。

---

# 第二轮返工（R3 + R4）· 3.5 寸单屏版

依据 `03-verify-a11y-35.md`（P0×1 · P1×6 · P2×9）与 `04-verify-hierarchy-35.md`。
两份报告有四条重合（R4-02=R3-01 / R4-03=R3-03 / R4-04=R3-02 / R4-05=R3-04），去重后
**共 23 条，修 21 条，跳过 2 条。** 修完的版本留档在 `design/variations-35-single.html`，
截图在 `design/shots/35-single/`。

## 一条方法论，先写在前面

R4 报告结尾那句说到点子上了：**R3-01 / R3-02 / R3-03 三条全部源自同一个修复 B1。**
「把轨道提亮到 --ash」这个动作同时改变了三对关系——轨道对底色、填充对轨道、
填充对 accent 语义——而我上一轮只验了第一对，第二对只在 ok 档验了，第三对完全没验。
所以这一轮我把断言从「某个元素的颜色对不对」改成了**相邻色的关系矩阵**：
每个变体 × 每个阈值档 × {轨道 / 填充 / 缺口 / 底色} 全配对，
accent 计数也从选择器白名单改成**按计算颜色全量扫描**——正是后者扫出了白名单看不见的
`.bar>i` 与 `.band .used`（它们是全屏面积最大的两块 accent 白）。

## 阻断与质量（全部修完）

**R3-01 P0 + R3-02 + R4-02 + R4-04 · 填充边界改用形状，不再靠色差**
danger 填充叠在提亮后的 ash 轨道上只有 1.81:1，而红黄是阈值语义、动不得。
按 lead 拍板：三种填充（V1 bar / V2 弧 / V3 时间带）的**末端统一加一道 2px `--canvas`
缺口**，与 V3 游标同机制。边界对比度因此恒定且与填充色无关——实测缺口对 ink 18.24、
对 charcoal 13.39、对 warn 12.68、对 danger 6.81、对裸轨道 3.75，**每一档都过 3:1**。
V2 的形状补偿同时从每侧 0.5px 提到每侧 2px（`--arc-stroke` 8px vs `--arc-track` 4px
= 0.32 mm ≈ 2 弧分，越过了约 1 弧分的分辨阈），并把端点从 `round` 改成 `butt`
（R4-15：round cap 会吃掉 97% 时仅剩的 1.4px 轨道，让 97% 与 100% 不可区分）。

**R3-03 + R4-03 · `--quota-fill` 从 `--ink` 降到 `--charcoal`**
填充与 accent 同 hex，且面积比未读点大 70 倍——「全屏最亮最大的那块白」是额度而不是
「有人在等你」。charcoal 对 ash 轨道 3.57:1 仍过线，同时与 accent 拉开半档。
这与 Q1 处理 `--id-codex` 是同一个手法。实测 accent 计数从 populated 3 / attention 4
回到 1 / 2。

**R3-04 + R4-05 + R4-10 · foot 槽位恒占位**
`:empty{display:none}` 改成 `visibility:hidden`，并且 foot 在 ok 档也有文案，
所以三块瓦片的仪表 / 数字回到同一条基线（原来错位 12 画布 px = 12 弧分）。
跨瓦片的基线一致性优先于单块瓦片的居中——前者是三块一起看时才成立的关系。

**R3-05 · V3 名字列 84px → 92px + 省略号**「Claude Code」实测需要 89px，原来溢出 5px，
把三条判语推成三个不同的左边缘。固定列宽 = 可预测的扫视路径。

**R3-06 · 例外清单重写**登记的 5 条全部失效、实际的 4 处一条没登记。收了 `.tile` 的
10px、`.attn-tag` 与 V3 lead 的 3px、V2 骨架的 6px，然后把 `tokens.css` §6 重写成当前
真实集合，并同步改了文件头那行自检。**声明与实现不符这类问题按阻断处理**，
和上一轮 B2 是同一种缺陷。

**R3-07 + R4-13 · loading 可见度**骨架从 `--surface-elevated`（1.03:1，等于不存在）
提到 `--stone`（1.97:1）；四处「读取中」只留事件区那一处可见，另三处转 sr-only —— 
`role="status"` 仍是四份（B2 不回退），但视觉上不再把同一句话印四遍。

**R3-08 + R3-10 + R4-07 · V2 的倒计时不再印两遍**倒计时归弧心独占，foot 在 ok 档退成
它的说明「后重置」（这同时解掉 R3-10「弧心是个没有标签的裸串」），非 ok 档只留判语。

**R3-09 + Q5 补齐 · 事件区 sr-only 标题按状态改写**原来硬编码「最近完成」，
读屏用户在 attention 态听到的区段标题与内容矛盾。现在 attention→「需要你」、
running→「进行中」、loading/empty/error→「事件」。

**R3-11** `.tile-foot` 补 `text-overflow:ellipsis`（原来是 hidden 但硬切）。
**R3-12** tokens.css 里「charcoal is dead weight」的注释已过期，改写。
**R3-15** 断言口径：`overflow:hidden` 的盒子判 scrollHeight，`overflow:visible` 的盒子
改判「子元素 bbox 不越父盒」——原来 18px 的松弛是在掩盖口径问题而不是解决它。
另外排除了旋转盒（V2 的弧 svg 带 rotate(135deg)，外接矩形不代表可见范围）和
`aria-hidden` 图标的 1.6px 描边（accent 计的是面积，不是描边）。

**R4-01 P0 · V3 放大行不再回落 cwd**自检、笔记、简报三处都写了「cwd 已砍」，
代码却在无 summary 时回落到 cwd。已去掉回落，无 summary 时不渲染副行。
**轮播版的任何页都不出现 cwd。**
**R4-11** attention 判语降回常规字重，否则与紧邻的名字在色、重、尺度上全一致，读成一串。
**R4-12** `%` 从 `--mute` 提到 `--body`，归属主数字而不是元数据层。
**R4-16** 未读点回到 6px——它是全屏唯一的纯白 accent 载体，5px 富余度太低。
**R4-17** 判语只留结论「按这个速度用不完 / 会提前用尽」，砍掉「落后时钟 N 点」
这个机制术语前缀（它在 12px 上本来也读不出）。
**R3-14** 验证脚本已落盘 `design/verify.mjs`（仓库仍无 commit，建档由 lead 决定）。

## 跳过的两条

**R3-13（瓦片边框 `--hairline` 1.33:1）** 报告自己写了「属于已拍板的视觉真源，
不建议擅改」。它决定的是「三块」这个分组读不读得出来，属于实机待验项，不在本轮改。

**R3-16（edge 态 6 条未读在屏上无痕迹）** `brief-m0-35.md` 明确写了「去掉未读计数文字」，
报告也说「不算违规」。轮播版把 C 页扩到 4 条，积压的可见度自然改善了一档；
是否要在时钟旁加一个未读数（报告建议的 `14:33 · 8`）留给 lead 拍板。

**R4-06（12px 在 55 cm 处认不出）** 不算跳过：它的执行范围是轮播版——lead 已把新的
字号表写进 `brief-m0-rotation.md`，见下一节。单屏版作为留档保持它当初依据的 12px 下限。

---

# 轮播版

## 收束：V1 还是 V3 这个问题消失了

前两轮我推荐 V3（7 寸）又改判 V1（3.5 寸），R4 独立地也推荐 V1，理由不同：
我的理由是「10px 的时间带在 50–60 cm 外读不出填充与游标的落差」，
R4 的理由是层级——V3 最亮最大的图形是额度填充而不是「需要你」。
**轮播版让这个取舍不存在了**：A 页就是 V1 的内容，B 页就是 V3 的内容，
它们不再抢同一块 218px 的画布，各自拿到一整屏。V2 的弧按简报作为 A 页的可选风格，
默认关，本版没做（见「没做的」）。

## 三页 + 一张专用页

| 页 | caption |
|---|---|
| **A 总览** | **还剩多少。** 三块瓦片横排，64px 百分比是全屏唯一能从座位上一眼读到的东西；7d 不显示，倒计时从 12px 提到 18px。停留 10s。 |
| **B 额度详情** | **够不够撑到重置。** 三条竖向时间带，填充与「现在」游标画在同一根轴上，右端是重置刻度；判语只留结论。停留 6s。 |
| **C 事件** | **谁刚做完。** 最新一条 20px 单行放大 + 三行 16px；没有未读计数、没有「另 N 条更早」。停留 6s；新事件进来立刻跳这里并停 20s。 |
| **attention** | **谁在等我。** 整屏专用页接管，轮播暂停直到解除。28px「等待你」+ 18px 说明 + 已等多久。 |

## 节奏取值的理由

- **A 10s / B 6s / C 6s（一轮 22s）。** 轮播最大的坑是「扫一眼时想看的不在屏上」。
  A 是回归页，让它占掉将近一半的周期，随机一瞥命中它的概率才够高（10/22 ≈ 45%）。
  B 与 C 的 6s 是「一行放大标题 + 两三行次级」读完所需，再短会读不完。
- **空闲加速 B/C 各 4s**：30 分钟无事件且无 agent 在跑时，屏上没有新信息，
  缩短非总览页等于把回归周期从 22s 压到 18s。4s 是简报的硬下限，不再往下压。
- **新事件钉住 20s**：比 C 的常规停留长三倍多，因为这时 C 页上有一条你还没看过的东西；
  期间再来一条会重置这 20s（否则连续到达会让第一条只露 2s）。
- **attention 不计时**：它不是一页而是一个状态，只有解除才走。

## 字号（按 `brief-m0-rotation.md` 的新表，覆盖 3.5 寸单屏版的下限）

任何文本 ≥14px，12 / 13px 全部废除。实测 21 组合 `minFont` 恒为 14。
A 页 64px / 16px / 18px · B 页 20px / 16px / 14px · C 页 20px / 16px / 14px ·
attention 页 28px / 18px。依据是 04 报告按 0.16 mm/px、55 cm 实算出的
「12px 落在认不出区」——轮播腾出的空间正好用来还这笔债。

## 新做的判断（简报没写的）

1. **待命页往左偏移而不是往右。** 往右偏移会把 `.pages` 的 scrollWidth 撑到 488
   （虽被 overflow 裁掉，但那是一个真的可横向滚动的区域）；向左的溢出在 LTR 下不进入
   滚动区。切页方向因此统一为「从左滑入」。
2. **A 页 foot 拆成两档字号**：倒计时 18px mono + 状态词 14px。合成一档的话
   「2h26m · 余量偏紧」在 123px 的瓦片内宽里放不下，而且数字与说明本就不该是同一层。
3. **attention 页三点全暗**，读作「当前不在轮播的三页里」——这本身就是状态信息。
   原来那行 `dots.hidden` 因为 `.dots` 显式 `display:flex` 根本没生效，
   等于代码与意图不符，已改成明确保留。
4. **B 页三条带平分可用高度**（不再固定 60px）：原来底部空掉 14px，而 ZCode 那条的
   原因句反而被裁了下缘。未连接 / 错误的原因句也并进判语那一行，省掉整整一行。
5. **页指示点带 `data-not-accent`**，断言里显式排除——简报写明「这不算 accent」，
   但它确实是 `--ink` 背景，全量扫描会把它算进去。规则要写在代码里，不能只写在文档里。

## ZCode 拿到真实额度之后

Key 已进 Keychain，`quota.json` 的 zcode 从 disconnected 变成 5h 61% / 7d 17% /
1mo MCP 8/1000（pro 档），两份 fixtures 都在 16:32:48 重新生成，时间原点跟着改到该时刻。
影响：**「未连接」从静止态退成 empty 态的一种**（Key 缺失时才出现）；
A 页三块瓦片、B 页三条时间带现在都有真实数据。
61% 落在 ok 区间但离 warn 只差 19 个点，正好用来验「三块都正常但有一块明显偏高」时
层级还读不读得出来——B 页上 ZCode 的填充明显比另两条长，而颜色仍是中性，
说明层级是靠长度而不是靠语义色在传，这是对的。
`1mo` 窗口按简报继续不显示（「不要在任何页上恢复被砍掉的内容」）。

## 验证

`design/verify.mjs`（已落盘，R3-14）· Playwright / Chromium / dpr2 ·
**4 页 × 7 状态 = 28 个组合**：

- 画布边界、纵向裁切 / 子元素越界、字号下限 **14**、heading ≥5、loading `role="status"`、
  时钟可见、accent ≤2（按计算颜色全量扫描，排除 `data-not-accent` 与 `aria-hidden` 图标）
- 轨道 3.64:1 · 缺口对填充 13.39:1 · 缺口对轨道 3.75:1 · 游标对轨道 3.75:1，全部 PASS
  （`fill-vs-track` 保留为参考值，边界已由缺口承担）
- **节奏断言**：token 取值 A ≥ B,C 且无一页 < 4s · 实测页序 a→b→c→a→b→c ·
  新事件后 180ms 已在 C 页 · attention 接管 1.5s 后仍在 attn 页且 meter 显示「轮播暂停」·
  解除后回到 A · reduced-motion 下切页 `transitionDuration` = 0s

**结果：28/28 通过，0 console error，0 布局问题，0 节奏问题。**
截图 `design/shots/page-{a,b,c,attention}.png`（960×540 @2x）。

## 没做的 / 没验的

1. **A 页的 V2 弧风格开关没做。** 简报写的是「可选…默认关」。A 页的职责是
   「一眼读到还剩多少」，在同一页里放两套视觉系统会稀释它；而且 V2 的弧在 A 页
   64px 数字的预算下放不进 149px 宽的瓦片。要做的话建议单独一轮。
2. **实机仍未验。** 轮播节奏（10/6/6 在座位上是快是慢）、64px 数字会不会反而过大、
   切页的横向滑动在 zoom 2 下是否顺滑，都只在 Chromium 截图上看过。
   **节奏这一项尤其只能实机定**——22s 一轮是推算不是实测。
3. **空闲加速那条规则没有真跑过 30 分钟。** 代码路径是活的（`lastActivity` +
   `sceneHasRunning()`），但断言只覆盖了 token 取值，没有把时钟快进到 30 分钟后。
4. **提示音仍未听过**（Playwright `--mute-audio`），只验证了不抛异常。
5. **R3-13 的瓦片边框 1.33:1**：轮播版每页元素更少，分组更靠位置而不是靠边框，
   风险比单屏版低，但仍是实机待验项。
6. **多页之后的「找不到」风险没有用户验证。** 三页轮播的核心假设是「A 回归得够快」，
   这个假设成立与否要看人实际用起来会不会去等页——原型里无法验证。

---

# R5 返工（发布前最后一轮）

依据 `05-verify-rotation.md`（P0×1 · P1×5 · P2×14）。**修 19 条，跳 2 条。**

## 修了什么

**R5-01 P0 · 非当前页 `inert`。** 隐藏页里的四个事件行原本仍在 Tab 序里，焦点会掉到
看不见的地方。`aria-hidden` 只挡无障碍树，挡不住 Tab——`inert` 才是主机制，两者都保留。
断言口径：非当前页内可聚焦元素数 = 0，28 格全过。

**R5-02 + R5-10 · 重置刻度改成出头刻痕。** 原来是带内一道 `--mute` 竖线，对 ash 轨道
1.95:1 不可辨；而且 97% 那种窗口末期会和「现在」游标挤成一个 4px 的明暗簇。
现在上下各出头 2px 落到带外的 `--surface` 上（7.09:1），靠位置和出头形状传信息，
也就再不会与带内的游标争同一段像素。这与游标、与填充末端缺口是同一套语汇。

**R5-03 + R5-15 · B 页 error / empty 回到两行**，「下一步」不再被单行省略号截掉；
同时只有标题与图标是 `--danger`，中性的「下一步」降到 `--body`——否则三条并排就是
整屏三行红字，语义色被当成了装饰。

**R5-04** C 页时间列 58 → 68px（「29 分钟前」不再撞图标）。
**R5-06** hover 规则排到属性规则之后，未读行与 attention 行才有 hover 反馈；
attention 行已在 surface-card 上没有升档余量，改用 1px `--ash` 内描边。
**R5-07** 「读取中」挪到顶栏——轮播之后 A/B/C 是三个不同的时刻，把唯一一处可见的
放在 C 页等于 A/B 的 loading 态一个字都没有。
**R5-08** B 页名字列 `inline-flex` 下 `text-overflow` 对匿名文本项不生效，省略号
「装了但没通电」；改 `min-width:112px` + 内层 span。
**R5-09** A 页 msg 态改 `flex-start`，三块共基线（原来最大差 19px）。
**R5-11** 100% 不再降到 48px（那低于 A 页 ≥64px 的下限，而「额度用尽」恰恰最该喊出来），
改成藏掉此时冗余的 `%`。
**R5-13** attention 接管时手动切页给 10s 宽限，控制栏才能当核对工具用。
**R5-14** `data-rotate` 同步（原来是个死属性且值是错的）。
**R5-18** `.chip` 行盒放开到 1.2。
**R5-19** tokens.css §10 标题行补一句「已由 §14 覆盖」。
**R5-20** attention 页兑现一处 accent——这一页就是「需要你」本身，原来却是全屏 0 处。
用与未读点同一套词汇（6px 圆点）放在卡片右上；做成**真实元素而不是伪元素**，
因为伪元素躲得过 accent 全量扫描，那这处 accent 在断言里就是隐形的。

**R5-05 · 例外清单第三次失配，这次做成断言。** 前两版都是手写清单，两次都和实现对不上。
现在 `design/verify.mjs` 每一格都扫描 stage 内全部计算出的 gap / rowGap / columnGap /
padding* / margin*，凡不落在 {2,4,8,12,16,24,32} 上一律报错。
**实测 off-scale 集合为空**，§6 已据此重写。
这条断言当场就抓到一个：`#topLoading` 的 `margin-right:auto` 被 `getComputedStyle`
解析成 **371.58px** 的 used value。所以顺带立了一条纪律——**不用 `margin:auto` 做分布**，
改显式 `grid-column`，语义也更准（左槽是状态，右槽是时钟）。

**R3-16（lead 拍板）** A / B 页时钟旁「16:32 · 2」，14px `--mute`，未读 >0 才出现；
C 页不显示。`--mute` 不是 accent 色，不计入 ≤2。

**R5-12** 按报告给的两条路里的「改声明」：A 页 foot 是定宽 flex 容器，倒计时 18px +
状态词 14px 两档字号已经把长度控死（最长 105px < 133px 内宽），`text-overflow` 对
flex 项本来也不生效。文件头那行自检已改成实话。

## 跳过两条

**R5-16（A 页数字上下各余 37px）** 报告自己给 0.50 置信、写了「不改也行」。
这块留白是「一页只回答一个问题」换来的，大数字四周的呼吸感正是 A 页相对单屏版的
增益；改成三段式会把它重新压回单屏版的密度。

**R5-17（骨架 1.97:1 仍低于 3:1）** 报告写明「不是新问题，只是没修干净」，
且 WCAG 对纯装饰骨架不强制 3:1。再往上提会越过 raycast 的 surface 阶梯；
留作 M1 实机项——座位上看一眼再决定要不要上 `--ash` 20% 叠层。

## 验收

`design/verify.mjs` · **4 页 × 7 状态 = 28 格**，每格跑：画布边界 / 纵向裁切与子元素越界 /
字号下限 14 / heading ≥5 / loading `role="status"` / 时钟可见 / accent ≤2（全量扫描）/
轨道与缺口对比度 / **间距栅格（新）** / **inert 可聚焦数 = 0（新）**。
外加 6 条节奏断言。

**结果：28/28 通过，off-scale 0，inert 0，0 console error，0 布局问题，0 节奏问题。**
四张截图已覆盖。

M0 的未验清单没有变化：实机节奏与字号手感、空闲加速的 30 分钟路径、提示音、
瓦片边框 1.33:1、骨架 1.97:1——全部留给 M1 装到真屏上再看。

---

# 实机反馈 1

用户把轮播版放到副屏上看了，两条反馈，已写进 `brief-m0-rotation.md`。

## 节奏：10/6/6 s → 每页 60 s

**原来的设计假设被实机否掉了。** 我当初给 A 页 10s、B/C 各 6s，理由写在笔记里：
「轮播最大的坑是扫一眼时想看的不在屏上，所以让总览页占掉将近一半的周期，
随机一瞥命中它的概率才够高」。这个推理的前提是**人会盯着屏等**——实机上不是。
副屏是余光扫的，页太快的结果是每一眼都撞在切换动画上，而不是撞在内容上。

改成 60s 之后，回归靠的不再是**频率**而是**打断**：新事件与 attention 会主动把该看的
推到眼前，定时轮播退成兜底。「空闲加速」也随之作废——既然不靠频率回归，
空闲时缩短停留就失去了意义，规则和 `--dwell-idle` 一起删掉了。
新事件跳 C 后改为停满一个 dwell（60s），再来一条重新计时。

断言相应加了一条硬的：**三页 dwell 的默认值必须都是 60000ms**，
且 `--dwell-idle` 不得再存在。页序测试仍然把 dwell 临时压短来跑——那只是为了在几秒内
观察机制，token 的默认值不跟着改。

## 纯净模式

标签栏、地址栏和原型自带的控制栏都会破坏副屏观感，所以加了「只有画布」这一档：
`?pure=1` 或任何时候按 `P` 进入，控制栏与说明全部隐藏，`body` 背景直接是 `--canvas`，
画布居中铺满视口，同时请求 Fullscreen。`Esc` 或再按 `P` 退出；用户直接按 Esc 退出
浏览器全屏时，`fullscreenchange` 会把视图同步退回调试态，不会卡在半纯净的状态。

两处是实现层面必须做的判断：

1. **Fullscreen API 必须由用户手势触发**，所以 `?pure=1` 打开时只能先切视觉、
   挂一行 14px 的「按任意键进入全屏」，按键后再请求——提示只出现这一次。
   直接按 `P` 进入时本身就是手势，不需要这一步。
2. **`P` 的守卫范围**。第一版写成「焦点在任何控件上就不响应 P」，结果勾选框和按钮
   拿到焦点后 P 就失灵了——而 harness 里根本没有文本输入。收窄成只给真正会打字的
   地方让路（contenteditable / textarea / 文本类 input）。**这条是断言抓出来的**，
   不是我自己发现的。

纯净模式下轮播强制开、忽略 `localStorage` 里的手动选页（进入时直接回 A 页）；
键盘 ack 与右下角指示点保留。控制栏里也加了「纯净全屏」按钮做同一件事。

## 验收

全部断言重跑：**28 格 × 10 项全过**（画布边界 / 裁切 / 字号 ≥14 / heading ≥5 /
role=status / 时钟 / accent ≤2 / 轨道对比度 / 间距栅格 / inert），
节奏 6 条全过，**纯净模式 8 条全过**（进入、隐藏控制栏与说明、body 背景 = --canvas、
轮播强制开、指示点保留、Esc 退出、`?pure=1` 的 14px 提示与按键后消失、
纯净模式下 Tab 到事件行且 Enter 能 ack）。0 console error。
截图按要求未重出（版面未变，只有节奏与模式开关变了）。

**仍未验：** 60s 这个新值同样是推算——它解决了「太快」，但会不会变成「太慢以至于
想看 B 页要等一分钟」，只有下一轮实机能回答。纯净模式的 Fullscreen 在 Electron 里
走的是另一套（`setSimpleFullScreen`），本版验的是浏览器行为。

---

# 手动切换（M0 最后一次改动）

自动轮播之外补了手动翻页。四种触发方式，一个共同的后果（暂停 120 s）。

## 触发

- **键盘** `←` / `→` 上一页 / 下一页，`↑` 或 `Home` 回 A。
- **指示点**点击直接跳页：**热区放大到 24×24，视觉仍是 4px**。做法是把点本身画成
  `button::after`，`button` 只负责命中——这样「看得见的」与「点得到的」可以各自取值，
  不必为了好点而把点画大。
- **边缘热区**：画布左右各 15% 宽，无常驻控件；鼠标移入才浮出一个 24px 的 monoline
  chevron，2 s 无移动即隐藏。副屏上常驻箭头是纯粹的视觉噪音，它一年里有 364 天不需要。
- Electron 版的全局快捷键 `⌃⌥←/→/↑` 属 M1，本版未做。

## 手动之后

手动切页后轮播暂停 `--manual-hold`（120 s），指示点左侧挂一个 14px 的「‖」。
这个标记不是装饰：没有它，用户按停之后再看到屏不动，第一反应会是「轮播坏了」。
120 s 到期后从**当前页**继续，不跳回 A——人停在哪一页就是他想看那一页。

**打断优先级不变**：手动暂停期间来了新事件仍跳 C 并重新计时（`holdUntil` 被清零），
attention 仍接管整屏。attention 页**不能**被手动切走——它需要被处理——但可以按 Enter
ack 解除，解除后回 A 并恢复轮播。纯净模式下以上全部可用。

## 两个由断言抓出来的 bug

1. **方向键守卫过宽。** 第一版写成「焦点在控制栏里就不响应方向键」，结果用户点完
   控制栏按钮之后焦点留在那儿，**第一次按 `→` 永远失效**。`<button>` 对方向键没有
   原生语义，收窄成只给真正会用方向键的控件（文本 input / textarea / select /
   contenteditable）让路。这与上一轮 `P` 键守卫是同一个错误，我犯了两次。
2. **「‖」不会自己消失。** 原来只在 `showPage()` 里更新，于是 120 s 到期后只要没有
   翻页动作，标记就一直挂着——正好是它最会误导人的那一刻。改到每 100 ms 的
   `updateMeter()` 里刷新。

还有两处是**断言自己的口径错**，一并记下来，因为它们都属于「测试比实现更容易骗人」：
测 Enter ack 时单按一次 Tab 只落到隔壁的控制栏按钮上（改成一路 Tab 到焦点真的进画布）；
判断 attention 是否解除时找的是「还有没有 attention 行」，但那一行 ack 之后本来就该
留在事件流里，判据应当是它不再 `unread`。

## 验收

全部断言重跑：28 格 × 10 项、节奏 6 条、纯净模式 9 条、**手动切换 14 条**——
`--manual-hold` = 120000 · `→`/`←`/`Home` 各自到位 · 「‖」出现 · 指示点热区 24×24
且视觉 4px · 边缘热区实测 15% · chevron 24px 且默认 opacity 0、hover 后 1、2 s 后隐藏 ·
手动切页后把 dwell 压到 0.3 s 也不自动翻页 · 手动暂停期间新事件仍跳 C ·
attention 下 `←`/`→` 无效 · attention 行 Enter 能 ack 解除 · 纯净模式下 `→` 仍可用
且不会退出纯净模式。**全部通过，0 console error。** 截图未重出（版面未变）。

## 交给 M1 的一句话

移植来源是 `design/variations.html`（轮播版）。tokens 全部在 `design/tokens.css`，
断言在 `design/verify.mjs`——**建议把它一起移植**：这一轮 M0 里有四个 bug 是断言抓到的
而不是我看出来的，其中两个（方向键守卫、「‖」不自清）在 Electron 里会原样复现。

---

# v2 · 实机反馈第二轮

按 `brief-m0-v2.md` 的六条改动重做。页序变成 **A → B → C1 → C2 → D → E**，
每页 60 s，C2 空时跳过，指示点最多 6 个。

## 每页一句 caption

| 页 | caption |
|---|---|
| **A 总览** | 还剩多少。三块瓦片横排，64px 百分比 + 18px 倒计时，7d 不显示。 |
| **B 额度详情** | 够不够撑到重置。三条时间带，填充与「现在」游标同轴；判语整句删掉，原位置改显示**当前窗口用得最多的模型名**。 |
| **C1 session** | 谁刚做完。最近 4 个 session，running 置顶，时间是**最后更新时间**不是开始时间。 |
| **C2 session** | 更早的 4 个。C1 满 4 行才溢出到这里；为空时整页跳过，指示点也少一个。 |
| **D 每日用量** | 我用得多不多、哪几天最猛。7 行 × N 周的格子，五档中性明度阶，←→ 移焦点。 |
| **E 今日 AI** | 今天最值得看的几件事。等权、不放大第一条——这一页是阅读不是扫视。不打断、不响铃、不计未读。 |

## 三个要解释的决定

**热力图配色：中性明度阶，不是绿也不是蓝。** 0 档用 `--surface-elevated`，1–4 档沿
`--charcoal` 方向的中性阶，顶档就是 `--charcoal` —— 也就是 `--quota-fill` 的颜色。
不用 GitHub 的绿是简报的要求；**不用 `--accent-blue` 是我的判断**：ZCode 的身份色
`#6f93e6` 就是蓝，一屏蓝格子加一个蓝图例点会让人以为格子按 agent 分了色，
而简报明确要求格子只按总量分档。用中性阶还有一层好处：它在这套系统里已经有稳定含义
（「这是数据，不是强调」），顶档不会被误读成 accent。

**模型名用 sans 不用 mono。** 模型名是专名不是数字，`Fable 5.1`、`gpt-6-astra` 混合
大小写与连字符，Inter 的字形区分比等宽好；mono 会把字母也拉成等宽，读起来像代码路径。
数字仍然一律 mono——这条分界线全项目一致：**数字用 Plex Mono，名字用 Inter**。
ZCode 的 `GLM-5.3-Flash` 是 `events.json` 里 `source` 字段的真实值；Codex 与 Claude
的会话记录里没有模型名字段，先用简报给的例子占位，M1 接真实数据时替换。

**tooltip 报三家各自的单位，不报一个「统一的请求次数」。** 简报希望统一成请求次数，
但 `usage.json` 的实际来源是 `codexbar cost` 的 **token 数**（Codex / Claude）与 ZCode
日志里的**请求条数**——三家拿不到同一个量。fixture 自己的 note 也写了「units differ
per agent」。所以分档用 `intensity`（每家各自归一化后取均值，0–1），tooltip 则如实写
`Codex 0 tok · Claude 2.3亿 tok · ZCode 233 次`。**编一个统一数字比不统一更糟**，
它会让人拿三家的数去比大小。

## 横向压缩补偿

用户用 `aspect-test.html` 的圆校准出系数 **1.19**。`--panel-x` 默认 1.19，画布逻辑宽
`round(480/1.19) = 403`，stage 从 `zoom` 换成 `transform: scale(2*panelX, 2)`——
zoom 做不到非等比。控制栏有 1.00–1.25 的滑块，纯净模式下 `[` / `]` 调 ±0.02 并在
右上角闪显 1 s。

一个已知的 1px：403 × 2.38 = 959.14，比 960 差 0.86px。这是画布宽取整带来的，
简报把缩放定义为 `2 × panelX`，我没有改成 `960 / 403` 去凑整——凑整会让「系数」
不再等于用户校准出来的那个数。实机上这 1px 落在最右列，可忽略；若要绝对满屏，
M1 里把 Electron 窗口宽设成 959 或把缩放改成 `960 / round(480/panelX)`。

## 断言抓到的四个 bug

这一轮又是断言先发现的：

1. **未读旗标打在了排序之前。** v2 引入「running 置顶 + 按最后更新倒序」之后，
   `unread: i === 0` 标的还是原始顺序的第一条，于是屏上第一行不是未读的那条，
   「只有最新那条未读拿全套强调」这条 accent 纪律失去了落点。改成排序后再打旗标。
2. **`dwellFor()` 还在认旧的页 id `"c"`。** C 拆成 C1/C2 之后，新事件跳到 C1 却拿不到
   `--dwell-event`，被常规 dwell 立刻翻走——「新事件停 60 s」整条规则失效。
3. **E 页第三列写成 `auto`**，`auto` 列按 max-content 定宽，超长来源名会把整个网格
   撑出画布（403 宽下必现）。改 `minmax(0,28%)`。
4. **C 页跨天时间「9/13 10:36」压到状态图标上**：14px mono 下要 84px，而列宽只有 68px。
   加宽到 88px 并补 `overflow:hidden` 兜底。

另外我自己又犯了两次已经立过规矩的错：D/E 的新 CSS 里用了 `margin-left:auto` 做分布
（上一轮刚立的禁忌），以及 3px / 6px 的离格间距。两者都是「间距栅格」那条断言当场
拦下来的——**这正是把清单改成断言的价值**。

## 验证

`design/verify.mjs` · **4 画布 × 7 页 × 7 态 = 196 格全过**
（480×270 / 480×320 / 403×270 / 403×320，后两者是 panelX 1.19 的实机几何）。
每格仍然跑：画布边界、纵向裁切与子元素越界、字号 ≥14、heading ≥5、`role="status"`、
时钟可见、accent ≤2（全量颜色扫描）、轨道与缺口对比度、间距栅格、inert。
另有节奏 6 条、手动切换 14 条、纯净模式 9 条，全部通过，0 console error。

截图 14 张：`page-{a,b,c,c2,d,e,attention}.png`（panelX 1.0 对照）与同名 `-x119`
（实机系数）。

**边界检查的口径又修了一次**：内联 span 被 `overflow:hidden` 的祖先夹住时，
它的 rect 超出画布不算越界。这类「测试比实现更容易骗人」的口径错，这一轮是第三次。

## 未验证

1. **AIHOT 今天只给了 3 条，不是 5 条。** 布局按 5 条设计（满 5 条时标题自动退回一行），
   但「5 条等权」的实际观感没见过。我没有补两条占位——编两条假新闻比少两条真新闻糟得多。
2. **热力图只有 8 周数据**，简报设想的是 26–28 周。格子居中放置以免右边那片空白读成
   「没加载出来」，但满 26 周时的密度与可读性没验过。
3. **D 页相邻档之间的对比度不满足 3:1**（五档中性阶做不到，GitHub 自己也做不到）。
   顶档对 0 档远超 3:1，单格的确切数值由 tooltip 承担——**颜色只负责分布，数字负责精确**。
   这一条要在实机上看一眼：0 档与 1 档在 0.16 mm/px 下是否还分得开。
4. **panelX 1.19 是用户在圆上校准的，不是我验的**；403 宽下六页无溢出是我验的。
5. C1/C2 的「8 个 session」来自 events.json 的前 8 条，真实会话数更多时的分页行为
   （第 9 条之后去哪）没有定义——M1 接真实数据时要补。

---

# v2 补丁 · E 页的时间

m3-executor 拿真实数据跑起来之后报了一条，是真的，而且是我的：

**E 页每条新闻的相对时间从来没渲染出来过。** `.e-meta` 是一个 `overflow:hidden` 的
28% 格子，里面塞的是「来源 · 时间」。今天三条真实 AIHOT 来源名——
「公众号：小红书技术（dots.llm）」「Gary Marcus：The Road to AI We Can Trust（RSS）」
「Hacker News：AI 热帖」——每一条都长到让省略号落在来源里，时间被整个吃掉。
fixtures 里看起来只是「来源被截短了」，实机上是**三行全都没有时间**。

错在把「可变长的字段」和「必须出现的字段」放进同一个可裁切的盒子里：
被裁掉的一定是排在后面那个。改法是让盒子变成 flex，时间 `flex:none` 永不收缩，
来源才是被省略的那一半。顺序仍按简报的「来源 · 时间」，时间因此天然右对齐成一列。
同时给新闻页一个专用的短相对时间（`newsWhen`）：AIHOT 的窗口是 24 小时，
通用 `whenFmt` 的跨天串「9/13 02:34」要 84px，在这一行里买不起；
现在最长是「23 小时前」约 56px，省下的宽度还给了标题。

**这个 bug 能活这么久，是因为没有任何断言看着它。** 我的检查只问「有没有溢出、
有没有 <14px」，没问「该出现的字段是不是真的出现了」。已补一条回归断言：
E 页每条的 `.e-time` 必须文本非空且宽度 ≥1px。这是这轮唯一一条「内容级」断言，
值得推广——溢出类断言只能证明版面没坏，证明不了信息还在。

顺带把 `TOP_MODEL` 三个值换成 m3 在实机上取到的真实值（Claude 是 `Opus 5`，
不是简报举例的 `Fable 5.1`）。原型没有模型 fixture，写死一个已知为假的名字
比写死一个静态的真名字更糟。

重跑：196/196 格通过，节奏 / 手动切换 / 纯净模式三套全清，0 console error。
`page-e.png` 与 `page-e-x119.png` 已重出。
